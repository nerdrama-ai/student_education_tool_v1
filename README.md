# StudyAid MVP — Full Repository

This is the complete source for the StudyAid MVP (frontend + backend).

- **Frontend** (Next.js + Tailwind): in `/frontend`
- **Backend** (FastAPI + OCR + OpenAI): in `/backend`

You can deploy this directly from root to Vercel (frontend) and Render (backend).
